<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Result</title>
</head>
<body>
    <h2>Form is submitted</h2>
    <p>Name: {{ name }}</p>
    <p>Email: {{ email }}</p>
    <p>Phone: {{ phone }}</p>
    <p>Address: {{ address }}</p>
    <p>Gender: {{ gender }}</p>
    <p>Age: {{ age }}</p>
    <p>Hobbies: {{ hobbies }}</p>
    <p>Preferred language: {{ language }}</p>
    <p>Comments: {{ comments }}</p>
    <p>Checkbox values: {{ checkbox_values }}</p>
    <p>Radio button value: {{ radio_value }}</p>
    <p>Select box value: {{ select_value }}</p>
    <p>File uploaded: {{ file_uploaded }}</p>
    <p>File size: {{ file_size }}</p>
    <p>File type: {{ file_type }}</p>
    <a href="/">Back to form</a>
    
</body>
</html>